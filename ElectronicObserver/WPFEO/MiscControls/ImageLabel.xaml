<UserControl x:Class="ElectronicObserver.WPFEO.MiscControls.ImageLabel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ElectronicObserver.WPFEO.MiscControls"
             mc:Ignorable="d" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             Background="{DynamicResource MaterialDesignDarkBackground}"
             FontFamily="{materialDesign:MaterialDesignFont}"
             x:Name="ImageLabelControl"
             d:DesignHeight="30" d:DesignWidth="100">

	<UserControl.ToolTip>
		<TextBlock x:Name="ToolTipText" />
	</UserControl.ToolTip>

	<StackPanel Orientation="Horizontal" Margin="5 0">
		<Image Source="{Binding ElementName=ImageLabelControl, Path=Source}" Margin="5"/>
		<local:OutlinedTextBlock Text="{Binding ElementName=ImageLabelControl, Path=Text}"/>
	</StackPanel>

</UserControl>
